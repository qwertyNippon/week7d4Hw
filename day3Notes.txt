No white board!!! Resume intro day

Dom means document
Get the id by text.

getElementById()
querySelector()
^^^ grabs the first thing it finds unless you add All
then it returns an array.

HTML DOM events can be listened for. every 16.6sec they could passively be changed.
document.createElement()
let fname = event.target[0].value;
let lname = event.target[1].value;
^^^ used to grab the value of a submitted element.

.fetch()    API
const res = fetch('url')
.then((res) -> {return res.json()}).then((data) ->{console.log(data)});
most common to see. fetch().then().then().catch()

const func = async () =>
